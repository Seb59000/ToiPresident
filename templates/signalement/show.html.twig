{% extends 'base.html.twig' %}

{% block title %}Signalement{% endblock %}

{% block body %}
    <h1>Signalement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ signalement.id }}</td>
            </tr>
            <tr>
                <th>Categorie</th>
                <td>{{ signalement.categorie }}</td>
            </tr>
            <tr>
                <th>Idobjtosuppr</th>
                <td>{{ signalement.idobjtosuppr }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ signalement.commentaire }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('signalement_index') }}">back to list</a>

    <a href="{{ path('signalement_edit', {'id': signalement.id}) }}">edit</a>

    {{ include('signalement/_delete_form.html.twig') }}
{% endblock %}
