<form method="post" action="{{ path('signalement_amendement_delete', {'id': signalement_amendement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ signalement_amendement.id) }}">
	<button class="btn">Effacer le signalement</button>
</form>
