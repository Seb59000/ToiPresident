<form method="post" action="{{ path('signalement_commentaire_delete', {'id': signalement_commentaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ signalement_commentaire.id) }}">
	<button class="btn">Effacer le signalement</button>
</form>
