<form method="post" action="{{ path('signalement_sujet_delete', {'id': signalement_sujet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ signalement_sujet.id) }}">
	<button class="btn">Effacer le signalement</button>
</form>
